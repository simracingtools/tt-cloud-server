env: flex
runtime: java
runtime_config:
  jdk: openjdk8 # use OpenJDK 8
#instance_class: B4

manual_scaling:
  instances: 1

resources:
  cpu: 4
  memory_gb: 4.0
  disk_size_gb: 10

handlers:
  - url: /.*
    script: this field is required, but ignored